# Gemini CLI project adapter

`AGENTS.md` is the canonical project policy.

@./AGENTS.md

Provider-specific Gemini subagents under `.gemini/agents/` are thin adapters to the shared role definitions under `.agents/roles/`.

Shared skills are under `.agents/skills/` and should be preferred over duplicate Gemini-only procedures.
