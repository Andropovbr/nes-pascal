program InvalidMetaspriteAnimation;

var
    Player: metasprite;

begin
    nes.import_metasprite(player);
    Player := nes.metasprite_create(player.idle_0);
    nes.metasprite_set_animation(Player, $00);
    nes.set_background_color($0F);
    nes.run;
end.
