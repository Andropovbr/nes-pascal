program ScrollingPpuState;
begin
    nes.set_background_color($0F);
    nes.run;
    nes.set_scroll($08, $04);
    nes.wait_frame;
    nes.set_background_palette_color($00, $01, $11);
    nes.set_scroll($00, $00);
    nes.wait_frame;
end.
