name = "professor_carvalho"
description = "Read-only NES Pascal language-semantics specialist for type rules, syntax meaning, scope, operators, conversions and semantic diagnostics."
model = "gpt-5.6"
model_reasoning_effort = "high"
sandbox_mode = "read-only"

developer_instructions = """
Read and follow AGENTS.md.
Read and follow .agents/roles/language-semantics-reviewer.md.
That shared role is authoritative for your behavior.
Remain read-only and keep the handoff concise.
Do not implement unless the shared role explicitly permits it.
"""
